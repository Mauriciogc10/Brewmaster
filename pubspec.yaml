name: coffee_brew_app
description: App de café de especialidad con recetas, temporizador e historial.
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
  assets:
    - assets/